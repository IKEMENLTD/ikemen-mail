{
  "name": "ikemen-mail",
  "version": "1.0.0",
  "private": true,
  "description": "Ikemen Mail — a simple email sender web app (Netlify Functions + static site, Supabase, Resend).",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2"
  },
  "devDependencies": {
    "netlify-cli": "^17"
  }
}
