{
  "name": "ikemen-mail",
  "version": "1.0.0",
  "private": true,
  "description": "Ikemen Mail — a simple email client web app with login (inbox/sent/drafts). Netlify Functions + static site, Supabase Auth + DB, Resend.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2"
  },
  "devDependencies": {
    "netlify-cli": "^17"
  }
}
